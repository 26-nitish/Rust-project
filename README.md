# Rust-project

## Run the backend
```bash
cd backend
cargo build --release  # build the project
cargo run --release    # run the project
```

## Run the frontend
```bash
cd frontend
python3 -m http.server 8000
``` 